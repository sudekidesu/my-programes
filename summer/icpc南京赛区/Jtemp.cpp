#include <bits/stdc++.h>
using namespace std;
const int maxn=10000000+100;
int num[maxn],flag[maxn];
vector<int> v;
void init()
{
    for(int i=2;i<maxn;i++)
    {
        if(flag[i]) continue;
        v.clear();
        int j=i,x=i;
        for(int k=2;k*k<n;k++)
        {
            while(n%i==0)
            {
                n/=i;
                v.push_back(i);
            }
        }
        for(int)
    }
}
int main()
{
    init();
}
